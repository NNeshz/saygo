{
  "name": "@saygo/backend-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "build": "bun --env-file=../../.env build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@saygo/api": "*",
    "elysia": "^1.4.28"
  },
  "devDependencies": {
    "@saygo/typescript-config": "*"
  }
}